package github.x1n32;

/**
 *  TASK 3: 
 *  Let's create a new Decorator class like Knight, this time, lets have a Wizard class. 
 *  Create a "WizardDecorator.java" that extends Character.
 *  Set its base magic to 20. Create a String method called magicSpell().
 */
public class WizardDecorator {
    
}
